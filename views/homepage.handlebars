{{#each posts as |post|}}
<a href="/posts/{{ post.id }}">
    <h2 class="float-left">{{ post.title }}</h2>
    <p class="float-right">Posted by: {{ user.name }} on {{ format_date date_created }}</p>
</a>
{{/each}}