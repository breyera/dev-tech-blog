{{#if posts.length }}
{{#each posts as |post|}}
<a href="/dashboard/edit/{{id}}">
    <div class="card">
        <div>
            <div class="c-header">
                <h3>{{post.title}}</h3>
                <p>Post on {{format_date date_created}}</p>
            </div>
            <div class="card-body">
                {{post.content}}
            </div>
        </div>
    </div>
</a>
{{/each}}
{{/if}}
<div>
    <div>
        <a href="/dashboard/newpost" type="button">+ New Content</a>
    </div>
</div>